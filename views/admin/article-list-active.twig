{% extends 'templates/default.twig' %}

{% block title %} RA | Reporting - App {% endblock %}

{% block headContent %} 
	Active Article <i style="color:green" class="fa fa-check-square-o"></i>
{% endblock %}

{% block content %}

<a href="{{ path_for('article-add') }}" class="btn btn-primary">Tambah</a>

<div class="row">
	<div class="col-xs-12">
		<div class="panel panel-default">
			<div class="panel-body">
				<form action="" method="POST">
					<table id="example2" class="table table-bordered table-striped table-hover">
						<thead>
							<tr>
								<th style="width: 40px">Delete</th>
								<th>Title</th>
								<th>Content</th>
								<th>Image</th>
								<th style="width: 40px">Edit</th>
							</tr>
						</thead>
						{% for article in article %}
						<tr>
						    <td class="text-center"><input type="checkbox" name="inactive[]" value="{{ article.id }}"></td>
							<td><a href="{{ path_for('article-read', { id:article.id }) }}">{{ article.title }}</a></td>
							<td>{{ article.content|slice(0, 200) }}</td>
							<td><img src="{{ base_url() }}/assets/images/{{ article.image }}" width="100" height="100"></td>
							<td class="text-center"><a href="{{ path_for('article-edit', { id:article.id }) }}"><i class="fa fa-pencil"></i></a></td>
						</tr>
						{% endfor %}
					</table>
					<hr style="margin-top:10px;margin-bottom:10px;" >
					<div class="panel-body" align="right">
						<button type="submit" name="active"  class="btn btn-warning"><i class="fa fa-trash-o fa-lg" aria-hidden="true"></i> Delete </button>
						<hr style="margin-top:10px;margin-bottom:10px;">
					</div>
				</form>
			</div>
		</div>
	</div>
</div>

{% endblock %}